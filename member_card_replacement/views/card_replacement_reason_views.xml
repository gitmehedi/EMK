<?xml version="1.0"?>
<odoo>
    <data>
        <record id="view_card_replacement_reason_form" model="ir.ui.view">
            <field name="name">member.card.replacement.reason.form</field>
            <field name="model">member.card.replacement.reason</field>
            <field name="priority" eval="1"/>
            <field name="arch" type="xml">
                <form string="Card Replacements Reason" duplicate="0">
                    <header>
                    <button name="act_approve" string="Approve" states="draft" type="object" class="oe_highlight"/>
                    <button name="act_draft" string="Draft" states="reject" type="object" class="oe_highlight"/>
                    <button name="act_reject" string="Reject" states="draft" type="object"/>
                    <field name="state" widget="statusbar" statusbar_colors='{"cancel":"red"}'/>
                </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
        </record>
        <record id="view_card_replacement_reason_tree" model="ir.ui.view">
            <field name="name">member.card.replacement.reason.tree</field>
            <field name="model">member.card.replacement.reason</field>
            <field name="priority" eval="1"/>
            <field name="arch" type="xml">
                <tree string="Card Replacements Reason" decoration-info="state=='draft'" decoration-success="state=='approve'"
                  decoration-danger="state=='reject'"
                  decoration-bf="state in ('approve',)">
                    <field name="name"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>

        <record id="view_member_card_replacement_reason_search" model="ir.ui.view">
        <field name="name">view.member.card.replacement.reason.search</field>
        <field name="model">member.card.replacement.reason</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="state"/>
                <group string="Group By">
                    <filter string="Pending" name="pending" icon="terp-personal"
                            domain="[('state','in',('draft','approve')),('pending','=',1),'|',('active','=',0),('active','=',1)]"/>
                    <filter string="Rejected" name="state" icon="terp-personal"
                            domain="[('active','=',0),('pending','=',0),('state','=','reject')]"/>
                    <filter string="In-Active" name="state" icon="terp-personal"
                            domain="[('active','=',0),('pending','=',0),('state','=','approve')]"/>
                </group>
            </search>
        </field>
    </record>

        <record model="ir.actions.act_window" id="action_card_replacement_reason">
            <field name="name">Card Replacements Reasons</field>
            <field name="res_model">member.card.replacement.reason</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to add a new Member's Card Replacements Reason
                </p>
            </field>
            <field name="context">
            {"search_default_active":'False',"search_default_pending":'True'}
        </field>
        </record>

        <menuitem id="menu_card_replacement_reason" sequence="150"
                  parent="membership.menu_marketing_config_association"
                  action="action_card_replacement_reason"
                  groups="member_user.group_mem_user_officer,membership_extension.group_membership_manager"/>

    </data>
</odoo>