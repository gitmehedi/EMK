<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data>
        <record id="view_form_indent_type" model="ir.ui.view">
            <field name="name">indent.type.form</field>
            <field name="model">indent.type</field>
            <field name="arch" type="xml">
                <form string="Indent Type" duplicate="0">
                     <header>
                        <button name="act_approve" string="Approve" states="draft" type="object" class="oe_highlight"/>
                        <button name="act_draft" string="Draft" states="reject" type="object" class="oe_highlight"/>
                        <button name="act_reject" string="Reject" states="draft" type="object"/>
                        <field name="state" widget="statusbar" statusbar_colors='{"cancel":"red"}'/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
        </record>

        <record id="view_tree_indent_type" model="ir.ui.view">
            <field name="name">indent.type.tree</field>
            <field name="model">indent.type</field>
            <field name="arch" type="xml">
                <tree string="Indent Type" decoration-info="state=='draft'" decoration-success="state=='approve'"
                      decoration-danger="state=='reject'"
                      decoration-bf="state in ('approve',)">
                    <field name="name"/>
                     <field name="active" invisible="1"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>

        <record id="view_search_indent_type" model="ir.ui.view">
            <field name="name">view.search.indent.type</field>
            <field name="model">indent.type</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name"/>
                    <field name="state"/>
                    <group string="Group By">
                        <filter string="Pending" name="pending" icon="terp-personal"
                                domain="[('state','in',('draft','approve')),('pending','=',1),'|',('active','=',0),('active','=',1)]"/>
                        <filter string="Rejected" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','reject')]"/>
                        <filter string="In-Active" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','approve')]"/>
                    </group>
                </search>
            </field>
        </record>

        <record model="ir.actions.act_window" id="action_indent_type">
            <field name="name">Indent Type</field>
            <field name="res_model">indent.type</field>
            <field name="view_mode">tree,form</field>
            <field name="context">
                {"search_default_active":'False',"search_default_pending":'True'}
            </field>
        </record>

        <menuitem id="menu_indent_type" name="Indent Type"
                  parent="stock.menu_stock_config_settings"
                  action="action_indent_type" sequence="35"
                  groups="stock.group_stock_manager"/>

    </data>
</odoo>