<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="res_partner_organizer_view_tree" model="ir.ui.view">
            <field name="name">view.res.partner.form.event.inherited</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <div name="button_box" position="inside">
                    <button class="oe_stat_button"
                            type="object"
                            icon="fa-ticket"
                            name="action_event_view" attrs="{'invisible': [('event_count','=', 0)]}">
                        <field string="Events" name="event_count" widget="statinfo"/>
                    </button>
                    <field name="organizer" invisible="True"/>
                </div>
            </field>
        </record>

        <record id="action_partner_organizer_form" model="ir.actions.act_window">
            <field name="name">Organizers</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <field name="view_type">form</field>
            <field name="domain">[('organizer','=',True)]</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="context">{'default_organizer': 1,'default_supplier': 0,
                'default_customer':0,'default_supplier': 0}
            </field>
            <field name="filter" eval="True"/>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to add a contact in your address book.
                </p>
                <p>
                    Odoo helps you easily track all activities related to
                    a supplier: discussions, history of purchases,
                    documents, etc.
                </p>
            </field>
        </record>


        <menuitem id="menu_partner_organizer" action="action_partner_organizer_form"
                  groups="event_user.group_event_administration" parent="event.menu_event_configuration"/>
    </data>
</odoo>