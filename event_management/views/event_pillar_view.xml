<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data>
        <record model="ir.ui.view" id="view_event_pillar_form">
            <field name="name">event.pillar.form</field>
            <field name="model">event.pillar</field>
            <field name="arch" type="xml">
                <form string="Event Pillar" duplicate="0">
                    <header>
                        <button name="act_approve" string="Approve" states="draft" type="object" class="oe_highlight"/>
                        <button name="act_draft" string="Draft" states="reject" type="object" class="oe_highlight"/>
                        <button name="act_reject" string="Reject" states="draft" type="object"/>
                        <field name="state" widget="statusbar" statusbar_colors='{"cancel":"red"}'/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                                <field name="event_pillar"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
        </record>

        <record model="ir.ui.view" id="view_event_pillar_tree">
            <field name="name">event.pillar.tree</field>
            <field name="model">event.pillar</field>
            <field name="arch" type="xml">
                <tree string="Event Pillar" decoration-info="state=='draft'" decoration-success="state=='approve'"
                      decoration-danger="state=='reject'"
                      decoration-bf="state in ('approve',)">
                    <field name="name"/>
                    <field name="event_pillar"/>
                    <field name="active" invisible="1"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>
        <record id="view_event_pillar_search" model="ir.ui.view">
            <field name="name">view.event.pillar.search</field>
            <field name="model">event.pillar</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name"/>
                    <field name="state"/>
                    <group string="Group By">
                        <filter string="Pending" name="pending" icon="terp-personal"
                                domain="[('state','in',('draft','approve')),('pending','=',1),'|',('active','=',0),('active','=',1)]"/>
                        <filter string="Rejected" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','reject')]"/>
                        <filter string="In-Active" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','approve')]"/>
                    </group>
                </search>
            </field>
        </record>

        <record model="ir.actions.act_window" id="action_event_pillar">
            <field name="name">Event Pillar</field>
            <field name="res_model">event.pillar</field>
            <field name="view_type">form</field>
            <field name="context">
                {"search_default_active":'False',"search_default_pending":'True'}
            </field>
        </record>


        <menuitem id="menu_event_pillar" action="action_event_pillar"
                  groups="event_user.group_event_officer" parent="event.menu_event_configuration"/>

    </data>
</odoo>