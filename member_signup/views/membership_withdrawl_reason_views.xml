<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="membership_withdrawal_reason_form_view" model="ir.ui.view">
        <field name="name">membership.withdrawal_reason.form</field>
        <field name="model">membership.withdrawal_reason</field>
        <field name="priority" eval="1"/>
        <field name="arch" type="xml">
            <form string="Membership Withdrawal" duplicate="0">
                <header>
                    <button name="act_approve" string="Approve" states="draft" type="object" class="oe_highlight"/>
                    <button name="act_draft" string="Draft" states="reject" type="object" class="oe_highlight"/>
                    <button name="act_reject" string="Reject" states="draft" type="object"/>
                    <field name="state" widget="statusbar" statusbar_colors='{"cancel":"red"}'/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="membership_withdrawal_reason_tree_view" model="ir.ui.view">
        <field name="model">membership.withdrawal_reason</field>
        <field name="inherit_id" ref="membership_withdrawal.membership_withdrawal_reason_tree_view"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="replace">
                <tree string="Membership withdrawal reasons" decoration-info="state=='draft'" decoration-success="state=='approve'"
                  decoration-danger="state=='reject'"
                  decoration-bf="state in ('approve',)">
                    <field name="name"/>
                    <field name="state"/>
                </tree>
            </xpath>
        </field>
    </record>

    <record id="view_membership_withdrawal_reason_search" model="ir.ui.view">
        <field name="name">view.membership.withdrawal_reason.search</field>
        <field name="model">membership.withdrawal_reason</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="state"/>
                <group string="Group By">
                    <filter string="Pending" name="pending" icon="terp-personal"
                            domain="[('state','in',('draft','approve')),('pending','=',1),'|',('active','=',0),('active','=',1)]"/>
                    <filter string="Rejected" name="state" icon="terp-personal"
                            domain="[('active','=',0),('pending','=',0),('state','=','reject')]"/>
                    <filter string="In-Active" name="state" icon="terp-personal"
                            domain="[('active','=',0),('pending','=',0),('state','=','approve')]"/>
                </group>
            </search>
        </field>
    </record>

    <record id="membership_withdrawal.membership_withdrawal_reason_action" model="ir.actions.act_window">
        <field name="name">Membership Withdrawal Reasons</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="context">
            {"search_default_active":'False',"search_default_pending":'True'}
        </field>
    </record>
</odoo>
