<?xml version="1.0"?>
<odoo>
    <record id="inherit_membership_products_tree" model="ir.ui.view">
        <field name="name">Add membership category field</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="membership.membership_products_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='type']" position="after">
                <field name="state" invisible="True"/>
                <field name="active" invisible="True"/>
                <field name="pending" invisible="True"/>
            </xpath>
            <xpath expr="//tree" position="attributes">
                <attribute name="decoration-info">state == 'draft'</attribute>
                <attribute name="decoration-success">state=='approve'</attribute>
                <attribute name="decoration-danger">state=='reject'</attribute>
                <attribute name="decoration-bf">state in ('approve',)</attribute>
            </xpath>
        </field>
    </record>

    <record id="inherit_membership_products_form" model="ir.ui.view">
        <field name="name">Add membership category field</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="membership.membership_products_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="replace">
                <form string="Membership products">
                    <header>
                        <button name="act_approve" string="Approve" type="object" states="draft"
                                class="oe_highlight"
                                groups=""/>
                        <button name="act_reject" string="Reject" type="object" states="draft"
                                groups=""/>
                        <button name="act_draft" string="Set to Draft" type="object" states="reject"
                                groups=""/>
                        <button name="act_inactive" string="Set to In-Active" type="object" states="approve"
                                groups=""/>
                        <button name="act_active" string="Set to Active" type="object" states="reject"
                                class="oe_highlight"
                                groups=""/>
                        <field name="state" widget="statusbar" invisible="True"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <label class="oe_edit_only" for="name" string="Membership Name"/>
                            <h1>
                                <field name="name"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="list_price" string="Membership Fee" required="True"/>
                                <field name="membership_status"/>
                                <field name="active"/>
                            </group>
                            <group>
                                <label for="membership_date_from" string="Membership Duration"/>
                                <div>
                                    <field name="membership_date_from" required="1" class="oe_inline"/>
                                    -
                                    <field name="membership_date_to" required="1" class="oe_inline"/>
                                </div>
                            </group>
                        </group>
                        <notebook>
                            <page string="General Information">
                                <group>
                                    <group>
                                        <field name="membership_category_id" string="Membership Category" required="1"
                                               options="{'no_open':True,'no_create':True}"
                                               domain="[('state','=','approve')]"/>
                                        <field name="default_code"/>
                                        <field name="categ_id" invisible="True"/>
                                        <field name="membership" invisible="1"/>
                                        <field name="company_id" groups="base.group_multi_company"
                                               options="{'no_create': True}"/>
                                    </group>
                                    <group>
                                        <field name="property_account_income_id" invisible="1"
                                               options="{'no_open':True,'no_create':True,'no_create_edit':True}"/>
                                        <field name="taxes_id" widget="many2many_tags" invisible="1"
                                               options="{'no_open':True,'no_create':True,'no_create_edit':True}"
                                               string="Taxes"/>
                                    </group>
                                </group>
                            </page>
                            <page string="Notes">
                                <group>
                                    <field name="description" placeholder="Add a description..."/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </xpath>
        </field>
    </record>

    <record id="inherit_membership_product_search" model="ir.ui.view">
        <field name="model">product.template</field>
        <field name="inherit_id" ref="membership.membership_product_search_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="replace">
                <search>
                    <field name="pending" string="Pending"/>
                    <field name="state" string="State"/>
                    <field name="active" string="Active"/>
                    <field name="name" string="Membership Product"/>
                    <group name="group_by">
                        <filter string="Pending" name="pending" icon="terp-personal"
                                domain="[('state','in',('draft','approve')),('pending','=',1),'|',('active','=',0),('active','=',1)]"/>
                        <filter string="Rejected" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','reject')]"/>
                        <filter string="In-Active" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','approve')]"/>
                        <filter string="By Type" name='type' domain="[]" context="{'group_by':'type'}"/>
                    </group>
                </search>
            </xpath>
        </field>
    </record>

    <record id="membership_signup_partner_form_view" model="ir.ui.view">
        <field name="name">Membership Extension Form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="membership_extension.view_partner_form"/>
        <field name="priority" eval="999"/>
        <field name="arch" type="xml">
            <xpath expr="//form/group/group/field[@name='category_id']" position="attributes">
                <attribute name="attrs">{'invisible':True}</attribute>
            </xpath>
        </field>
    </record>

    <record id="membership.action_membership_products" model="ir.actions.act_window">
        <field name="name">Membership Types</field>
        <field name="context">
            {'membership':True, 'type':'service', 'default_membership': True,
            'default_type':'service',"search_default_parent_id":1,"search_default_active":'False',"search_default_pending":'True'}
        </field>
    </record>

    <record id="membership_extension.menu_marketing_config_association_item" model="ir.ui.menu">
        <field name="name">Membership Types</field>
    </record>

</odoo>