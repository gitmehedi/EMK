<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="hr_attendance_terminal_form" model="ir.ui.view">
            <field name="name">hr.attendance.terminal.form</field>
            <field name="model">hr.attendance.terminal</field>
            <field name="type">form</field>
            <field name="arch" type="xml">
                <form delete="0" duplicate="0">
                    <header>
                        <button name="act_approve" string="Approve" states="draft" type="object" class="oe_highlight"/>
                        <button name="act_draft" string="Draft" states="reject" type="object" class="oe_highlight"/>
                        <button name="act_reject" string="Reject" states="draft" type="object"/>
                        <field name="state" widget="statusbar" statusbar_colors='{"cancel":"red"}'/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                                <field name="code"/>
                            </group>
                            <group>
                                <field name="device_type"/>
                                <field name="is_attendance"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers" groups="base.group_user"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
        </record>

        <record id="hr_attendance_terminal_tree" model="ir.ui.view">
            <field name="name">hr.attendance.terminal.tree</field>
            <field name="model">hr.attendance.terminal</field>
            <field name="arch" type="xml">
                <tree string="Attendance Terminal" decoration-info="state=='draft'"
                      decoration-success="state=='approve'"
                      decoration-danger="state=='reject'"
                      decoration-bf="state in ('approve',)">
                    <field name="name"/>
                    <field name="code"/>
                    <field name="device_type"/>
                    <field name="is_attendance"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>

        <record id="hr_attendance_terminal_search" model="ir.ui.view">
            <field name="name">hr.attendance.terminal.search</field>
            <field name="model">hr.attendance.terminal</field>
            <field name="type">search</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name"/>
                    <field name="code"/>
                    <field name="device_type"/>
                    <group string="Group By">
                        <filter string="Pending" name="pending" icon="terp-personal"
                                domain="[('state','in',('draft','approve')),('pending','=',1),'|',('active','=',0),('active','=',1)]"/>
                        <filter string="Rejected" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','reject')]"/>
                        <filter string="In-Active" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','approve')]"/>
                    </group>
                </search>
            </field>
        </record>

        <record id="action_hr_attendance_terminal" model="ir.actions.act_window">
            <field name="name">Attendance Terminal</field>
            <field name="res_model">hr.attendance.terminal</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{"search_default_active":'False',"search_default_pending":'True'}</field>
        </record>

        <menuitem id="menu_hr_attendance_terminal" name="Attendance Terminal" sequence="10"
                  parent="hr_attendance.menu_hr_attendance_settings" action="action_hr_attendance_terminal"
                  groups="hr_attendance.group_hr_attendance_manager"/>
    </data>
</odoo>