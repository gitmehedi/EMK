<?xml version="1.0"?>
<odoo>
    <record id="view_event_refund_payment_form" model="ir.ui.view">
        <field name="name">event.refund.payment.form</field>
        <field name="model">event.refund.payment</field>
        <field name="priority" eval="1"/>
        <field name="arch" type="xml">
            <form string="Invoice Payment">
                <header>
                    <button string='Register Payment' name="invoice_payment" type='object' class="oe_highlight"
                            states="open"/>
                    <field name="state" widget="statusbar" statusbar_visible="open,paid" no_label="1"/>
                </header>
                <sheet>
                    <h1 class="oe_title">
                        Session:
                        <field name="session_id" options="{'no_open':True,'no_create':True,'no_create_edit':True}"/>
                    </h1>
                    <group>
                        <group>
                            <field name="event_id"
                                   options="{'no_open':True,'no_create':True,'no_create_edit':True,}"/>
                            <field name="payment_partner_id"
                                   options="{'no_open':True,'no_create':True,'no_create_edit':True,}"/>
                            <field name="due_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                            <field name="paid_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                        </group>
                        <group>
                            <field name="journal_id"
                                   options="{'no_open':True,'no_create':True,'no_create_edit':True,}"/>
                            <field name="date"/>
                            <field name="payment_ref"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_event_refund_payment_tree" model="ir.ui.view">
        <field name="name">event.refund.payment.tree</field>
        <field name="model">event.refund.payment</field>
        <field name="priority" eval="1"/>
        <field name="arch" type="xml">
            <tree string="Invoice Payment" decoration-bf="state == 'open'"
                  decoration-success="state == 'open'" decoration-muted="state == 'paid'">
                <field name="session_id"/>
                <field name="event_id"/>
                <field name="payment_partner_id"/>
                <field name="payment_ref"/>
                <field name="journal_id"/>
                <field name="date"/>
                <field name="state"/>
                <field name="paid_amount" sum="paid_amount"/>
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_event_refund_payment">
        <field name="name">Event Refund Payments</field>
        <field name="res_model">invoice.payment</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to add a new Invoice Payments
            </p>
        </field>
    </record>

    <menuitem id="menu_event_refund_payment" parent="sub_event_payment"
              action="action_event_refund_payment" sequence="15"
              groups="member_user.group_mem_user_officer,event_user.group_event_officer"/>


</odoo>