<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="inherit_view_move_tree_ogl" model="ir.ui.view">
            <field name="name">account.move.tree</field>
            <field name="model">account.move</field>
            <field eval="8" name="priority"/>
            <field name="arch" type="xml">
                <tree decoration-info="state == 'draft'" decoration-muted="state == 'posted'"
                      decoration-bf="state in ('posted')" create="0">
                    <field name="date"/>
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="ref"/>
                    <field name="journal_id"/>
                    <field name="amount" sum="Total Amount"/>
                    <field name="state"/>
                    <field name="currency_id" invisible="1"/>
                    <button name="not_sync" string="Not Sync" type="action"
                            icon="fa-times-circle text-danger"
                            attrs="{'invisible': [('is_sync', '=', True)]}"/>
                    <button name="sync" string="Sync" type="action"
                            icon="fa fa-check text-success"
                            attrs="{'invisible': [('is_sync', '=', False)]}"/>
                    <field name="is_sync" invisible="1"/>
                </tree>
            </field>
        </record>


        <record id="account.action_account_move_reversal_ogl" model="ir.values">
            <field name="model_id" ref="account.model_account_move"/>
            <field name="name">Reverse Moves</field>
            <field name="key2">client_action_multi</field>
            <field name="value" eval="'ir.actions.act_window,' +str(ref('account.action_view_account_move_reversal'))"/>
            <field name="key">action</field>
            <field name="model">account.move</field>
            <field name="groups_id"
                   eval="[(6, 0, [ref('base.group_system')])]"/>
        </record>

        <record id="view_account_move_filter_ogl" model="ir.ui.view">
            <field name="name">account.move.select</field>
            <field name="model">account.move</field>
            <field name="arch" type="xml">
                <search string="Search Move">
                    <field name="name" filter_domain="['|', ('name','ilike',self), ('ref','ilike',self)]"
                           string="Move"/>
                    <field name="date"/>
                    <filter string="Not Synced" name="is_sync" domain="[('is_sync','=',0)]"/>
                    <filter string="Synced" domain="[('is_sync','=',True)]"/>
                    <filter string="Unposted" domain="[('state','=','draft')]" help="Unposted Journal Entries"/>
                    <filter string="Posted" domain="[('state','=','posted')]" help="Posted Journal Entries"/>
                    <separator/>
                    <filter string="Sales" domain="[('journal_id.type','=','sale')]"
                            context="{'default_journal_type': 'sale'}"/>
                    <filter string="Purchases" domain="[('journal_id.type','=','purchase')]"
                            context="{'default_journal_type': 'purchase'}"/>
                    <filter string="Bank Operations" domain="[('journal_id.type','=','bank')]"
                            context="{'default_journal_type': 'bank'}"/>
                    <filter string="Cash Operations" domain="[('journal_id.type','=','cash')]"
                            context="{'default_journal_type': 'cash'}"/>
                    <filter string="Miscellaneous Operations" domain="[('journal_id.type','=','general')]"
                            name="misc_filter" context="{'default_journal_type': 'general'}"/>
                    <separator/>
                    <field name="partner_id"/>
                    <field name="journal_id"/>
                    <field name="dummy_account_id"/>
                    <group expand="0" string="Group By">
                        <filter string="CBS Synchronize" context="{'group_by': 'is_sync'}"/>
                        <filter string="Partner" domain="[]" context="{'group_by':'partner_id'}"/>
                        <filter string="Journal" domain="[]" context="{'group_by':'journal_id'}"/>
                        <filter string="States" domain="[]" context="{'group_by':'state'}"/>
                        <filter string="Date" domain="[]" context="{'group_by':'date'}"
                                help="Journal Entries by Month"/>
                    </group>
                </search>
            </field>
        </record>

        <record id="action_move_journal_line_ogl" model="ir.actions.act_window">
            <field name="name">Journal Entries</field>
            <field name="res_model">account.move</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="account.view_move_tree"/>
            <field name="search_view_id" ref="view_account_move_filter_ogl"/>
            <field name="context">{"search_default_is_sync":1}</field>
            <field name="domain">[('is_cbs','=',False),('state','=','posted')]</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to create a journal entry.
                </p>
                <p>
                    A journal entry consists of several journal items, each of
                    which is either a debit or a credit transaction.
                </p>
                <p>
                    Odoo automatically creates one journal entry per accounting
                    document: invoice, refund, vendor payment, bank statements,
                    etc. So, you should record journal entries manually only/mainly
                    for miscellaneous operations.
                </p>
            </field>
        </record>

        <record id="action_move_journal_line_ogl_tree" model="ir.actions.act_window.view">
            <field name="sequence" eval="1"/>
            <field name="view_mode">tree</field>
            <field name="view_id" ref="inherit_view_move_tree_ogl"/>
            <field name="act_window_id" ref="action_move_journal_line_ogl"/>
        </record>

        <menuitem
                action="action_move_journal_line_ogl" name="OGL"
                id="menu_action_move_journal_line_ogl_form"
                parent="account.menu_action_move_journal_line_form"
                groups="account.group_account_user"
                sequence="10"/>
    </data>
</odoo>