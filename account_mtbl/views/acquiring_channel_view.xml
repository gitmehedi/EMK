<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_acquiring_channel_form" model="ir.ui.view">
        <field name="name">acquiring.channel.form</field>
        <field name="model">acquiring.channel</field>
        <field name="arch" type="xml">
            <form string="Acquiring Channel" edit="0" duplicate="0">
                <header>
                    <button name="act_approve" string="Approve" type="object" states="draft" class="oe_highlight"
                            groups="mtbl_access.group_mtbl_sys_admin"/>
                    <button name="act_reject" string="Reject" type="object" states="draft"
                            groups="mtbl_access.group_mtbl_sys_admin"/>
                    <button name="%(action_acquiring_channel_wizard)d" string="Edit" type="action"
                            class="oe_highlight" groups="mtbl_access.group_mtbl_sys_admin"
                            attrs="{'invisible':['|',('state','in',('draft','reject')),('pending','=', True)]}"/>
                    <button name="act_approve_pending" string="Approve" type="object" class="oe_highlight"
                            groups="mtbl_access.group_mtbl_sys_admin"
                            attrs="{'invisible':['|',('state','=','draft'),('pending','=', False)]}"/>
                    <button name="act_reject_pending" string="Reject" type="object"
                            groups="mtbl_access.group_mtbl_sys_admin"
                            attrs="{'invisible':['|',('state','=','draft'),('pending','=', False)]}"/>
                    <field name="state" widget="statusbar" invisible="True"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="pending" invisible="True"/>
                        </group>
                        <group>
                            <field name="code"/>
                        </group>
                    </group>
                    <field name="line_ids" states="approve">
                        <tree decoration-info="state=='pending'" decoration-success="state=='approve'"
                              decoration-bf="state in ('pending',)"
                              decoration-danger="state=='reject'">
                            <field name="change_name"/>
                            <field name="change_date"/>
                            <field name="state"/>
                            <field name="status" invisible="True"/>
                        </tree>
                    </field>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_acquiring_channel_tree" model="ir.ui.view">
        <field name="name">acquiring.channel.tree</field>
        <field name="model">acquiring.channel</field>
        <field name="arch" type="xml">
            <tree string="Acquiring Channel" decoration-info="state=='draft'" decoration-success="state=='approve'"
                  decoration-danger="state=='reject'"
                  decoration-bf="state in ('approve',)" duplicate="0">
                <field name="name"/>
                <field name="code"/>
                <field name="state"/>
            </tree>
        </field>
    </record>
    <record id="view_acquiring_channel_search" model="ir.ui.view">
        <field name="name">acquiring.channel.search</field>
        <field name="model">acquiring.channel</field>
        <field name="arch" type="xml">
            <search string="Acquiring Channel">
                <field name="name" string="Name"/>
                <field name="code" string="Code"/>
                <group string="Group By">
                    <filter string="Pending" name="pending" icon="terp-personal"
                            domain="['|',('active','=',0),('pending','=',1),('state','in',('draft','approve'))]"/>
                    <filter string="Rejected" name="state" icon="terp-personal"
                            domain="['|',('active','=',0),('pending','=',0),('state','=','reject')]"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_acquiring_channel" model="ir.actions.act_window">
        <field name="name">Acquiring Channel</field>
        <field name="res_model">acquiring.channel</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="context">
            {"search_default_active":'False',"search_default_pending":'True',}
        </field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to start a new acquiring channel.
            </p>
        </field>
    </record>

    <menuitem action="action_acquiring_channel"
              id="menu_action_acquiring_channel_tree"
              groups="mtbl_access.group_mtbl_sys_admin" sequence="17"
              parent="account.account_account_menu"/>

</odoo>
