<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="inherit_hr_recruitment_stage_tree_view" model="ir.ui.view">
            <field name="name">inherit.hr.recruitment.stage.tree.view</field>
            <field name="model">hr.recruitment.stage</field>
            <field name="inherit_id" ref="hr_recruitment.hr_recruitment_stage_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='name']" position="after">
                    <field name="state"></field>
                </xpath>
            </field>
        </record>

        <record model="ir.ui.view" id="inherit_hr_recruitment_stage_form_view">
            <field name="name">inherit.hr.recruitment.stage.form.view</field>
            <field name="model">hr.recruitment.stage</field>
            <field name="inherit_id" ref="hr_recruitment.hr_recruitment_stage_form"/>
            <field name="arch" type="xml">
                <xpath expr="//form" position="replace">
                    <form string="Stage" duplicate="0">
                        <header>
                            <button name="act_approve" string="Approve" states="draft" type="object"
                                    class="oe_highlight"/>
                            <button name="act_draft" string="Draft" states="reject" type="object" class="oe_highlight"/>
                            <button name="act_reject" string="Reject" states="draft" type="object"/>
                            <field name="state" widget="statusbar" statusbar_colors='{"cancel":"red"}'/>
                        </header>
                        <sheet>
                            <group>
                                <group>
                                    <field name="name"/>
                                </group>
                            </group>
                        </sheet>

                    </form>
                </xpath>
            </field>
        </record>

        <record id="inherit_hr_recruitment_stage_search_view" model="ir.ui.view">
            <field name="name">inherit.hr.recruitment.stage.search.view</field>
            <field name="model">hr.recruitment.stage</field>
            <field name="arch" type="xml">
                <search>
                    <field name="state"/>
                    <group string="Group By">
                        <filter string="Pending" name="pending" icon="terp-personal"
                                domain="[('state','in',('draft','approve')),('pending','=',1),'|',('active','=',0),('active','=',1)]"/>
                        <filter string="Rejected" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','reject')]"/>
                        <filter string="In-Active" name="state" icon="terp-personal"
                                domain="[('active','=',0),('pending','=',0),('state','=','approve')]"/>
                    </group>
                </search>
            </field>
        </record>

        <record model="ir.actions.act_window" id="hr_recruitment.hr_recruitment_stage_act">
            <field name="context">
                {"search_default_active":'False',"search_default_pending":'True'}
            </field>
        </record>

    </data>
</odoo>