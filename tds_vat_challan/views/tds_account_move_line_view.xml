<odoo>
    <data>
        <record id="view_tds_acc_move_line_tree" model="ir.ui.view">
            <field name="name">tds.account.move.line.tree</field>
            <field name="model">account.move.line</field>
            <field name="arch" type="xml">
                <tree string="Journal Items" create="false" delete="false">
                    <field name="date" string="Bill Date"/>
                    <field name="name" string="TDS/VAT Description"/>
                    <field name="statement_id" invisible="1"/>
                    <field name="partner_id" string="Vendor"/>
                    <field name="credit" sum="Total Credit" string="Amount"/>
                </tree>
            </field>
        </record>

        <record id="action_tds_account_moves" model="ir.actions.act_window">
            <field name="name">Journal Items</field>
            <field name="res_model">account.move.line</field>
            <field name="view_id" ref="view_tds_acc_move_line_tree"/>
            <field name="view_mode">tree</field>
        </record>

        <act_window id="action_account_invoice_confirm1"
                    multi="True"
                    key2="client_action_multi" name="Generate"
                    groups = "account.group_account_manager"
                    res_model="tds.challan.ou.selection.wizard" src_model="account.move.line"
                    view_mode="form" target="new" view_type="form"

        />


    </data>
</odoo>