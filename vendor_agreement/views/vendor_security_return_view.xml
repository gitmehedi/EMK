<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!--form view-->
        <record id="vendor_security_return_form" model="ir.ui.view">
            <field name="name">vendor.security.return.form</field>
            <field name="model">vendor.security.return</field>
            <field name="type">form</field>
            <field name="arch" type="xml">
                <form string="Vendor Security Return">
                    <header>
	                    <button string="Confirm" name="action_confirm" states="draft"
                                type="object" class="oe_highlight"/>
						<button string="Validate" name="action_done" type="object" class="oe_highlight"
								attrs="{'invisible': [('state', '!=', 'confirm')]}"/>
                     	<button string="Cancel" name="action_cancel" states="confirmed" type="object"
                                attrs="{'invisible': [('state', '!=', 'confirm')]}" confirm="Are you sure you want to Cancel this?"/>
                    	<field name="state" widget="statusbar" statusbar_visible="draft,confirm,done"/>
                	</header>
                    <sheet>
                        <group>
                            <group>
                                <field name="partner_id"
                                       options="{'no_open': True, 'no_create': 1, 'no_create_edit': 1}"/>
                                <field name="vsd_ids" widget="many2many_tags"/>
                            </group>
                            <group>
                                <field name="amount"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Return Line">
                                <field name="line_ids" nolabel="1">
                                    <tree string="Return Line" create="false">
                                        <field name="return_id"/>
                                        <field name="vsd_id"/>
                                        <field name="date"/>
                                        <field name="amount"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                        <div class="oe_chatter">
							<field name="message_follower_ids" widget="mail_followers"/>
							<field name="message_ids" widget="mail_thread"/>
	                	</div>
                    </sheet>
                </form>
            </field>
        </record>

        <!--tree view-->
        <record id="vendor_security_return_tree" model="ir.ui.view">
            <field name="name">vendor.security.return.tree</field>
            <field name="model">vendor.security.return</field>
            <field name="type">tree</field>
            <field name="arch" type="xml">
                <tree string="Vendor Security Return">
                    <field name="name"/>
					<field name="partner_id"/>
                    <field name="amount"/>
					<field name="state"/>
                </tree>
            </field>
        </record>
        <!--search view-->
        <record id="vendor_security_return_search" model="ir.ui.view">
            <field name="name">vendor.security.return.filter</field>
            <field name="model">vendor.security.return</field>
            <field name="arch" type="xml">
                <search string="Search Vendor Security Return">
                    <filter name="draft_sd" string="Pending" domain="[('state','=','confirm')]"/>
                	<separator/>
                    <filter name="confirm_sd" string="Confirmed" domain="[('state','=','confirm')]"/>
                	<separator/>
                    <filter name="approved_sd" string="Approved" domain="[('state','=','done')]"/>
				</search>
            </field>
        </record>

        <!--action view-->
        <record id="action_vendor_security_return_view" model="ir.actions.act_window">
            <field name="name">Vendor Security Return</field>
            <field name="res_model">vendor.security.return</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="vendor_security_return_search"/>
            <field name="context">{}</field>
        </record>
        <!--sequence for name-->
        <record id="seq_vendor_security_return" model="ir.sequence">
            <field name="name">Vendor Security Return</field>
            <field name="code">vendor.security.return</field>
			<field name="active">TRUE</field>
            <field name="prefix">VSR/%(range_year)s/</field>
            <field name="padding">5</field>
			<field name="number_increment">1</field>
        </record>

        <menuitem id="menu_vendor_security_return" name="Vendor Security Return"
                  parent="vendor_agreement.menu_agreements" action="action_vendor_security_return_view" sequence="60"
                  groups="vendor_agreement.group_vendor_agreement_manager,vendor_agreement.group_vendor_agreement_user"/>
    </data>
</odoo>