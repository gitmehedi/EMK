<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="delivery_schedule_email_template" model="mail.template">
            <field name="name">E-mail template for Delivery Instruction</field>
            <field name="model_id" ref="delivery_schedules.model_delivery_schedules"/>
            <field name="report_template" ref="delivery_schedules_report"/>
            <field name="report_name">RFQ_${(object.name or '').replace('/','_')}</field>
            <field name="auto_delete" eval="True"/>
            <field name="body_html">
                <![CDATA[<html>
                <head></head>
                <body style="margin: 0; padding: 0;">
                Good Job!
                <p>${object.name}</p>

                </body>
                </html>
            ]]></field>
        </record>
    </data>
</odoo>