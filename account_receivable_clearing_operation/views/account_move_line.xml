<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <record model="ir.ui.view" id="inherited_ac_mv_line_clearing_ac">
            <field name="name">inherited.account.move.line.clearing.view</field>
            <field name="model">account.move.line</field>
            <field name="inherit_id" ref="account.view_move_line_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="is_clearing_journal_entry" invisible="0"/>
                </xpath>

            </field>
        </record>

        <record id="unreconciled_acc_move_line_tree" model="ir.ui.view">
            <field name="name">account.move.line.tree</field>
            <field name="model">account.move.line</field>
            <field name="type">tree</field>
            <field name="arch" type="xml">
                <tree string="Account Move Line" edit="false" create="false" delete="false">
                    <button name="action_reconcile_journal_entry" class="oe_highlight" icon="fa-check-square"
                            type="object"/>

                    <field name="date"/>
                    <field name="move_id"/>
                    <field name="partner_id"/>
                    <field name="journal_id"/>
                    <field name="credit" string="Reconcile Amount"/>

                </tree>
            </field>
        </record>

         <!--<field name="domain">[-->
        <!--('my_flag', '=', True)-->
    <!--]</field>-->

    </data>
</odoo>